import Button from "./Button";

const Datepicker = () => {
  return (
    <div className="date-picker">
      <input type="date" />
      <Button text="send" />
    </div>
  );
};

export default Datepicker;
